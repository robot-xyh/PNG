# Betaflight 推力 LUT 现有数据覆盖审计

## 1. 结论

现有带桨飞行数据**不能直接生成发布用推力 LUT**。数据量已经足够，但电压和油门的二维覆盖
不完整：有效覆盖为 `22.25--25.11 V / 1200--1500 us`，没有 `20--22 V` 的碰撞前有效样本，
`1350 us` 以上也只有 `18` 个 10 Hz 时间桶。

当前结果只允许确定“需要补哪些区间”，不能替换
`config/betaflight.rk3588.velocity_png.flight_supervised.json` 中的
`PENDING_FULL_6S_THRUST_LUT`，也不能据此生成主动飞行批准文件。

## 2. 审计方法

输入清单为
`config/betaflight.thrust_coverage_sources.20260904.json`，包含 15 个可唯一配对的主机 CSV 与
Blackbox BFL。使用 Blackbox tools 提交
`f832acf9cd9dbe5ad8220de1a5f4eb4021523d72` 解码；主机 `rc_in_ch4` 与 Blackbox
`rcCommand[3]` 通过油门波形对齐。

Blackbox 约 `800 Hz` 的连续行不能当作独立样本计数。审计先按每架次 `0.1 s` 时间桶取各字段
中位数，形成 10 Hz 有效样本，再保留以下范围：

- 电压 `20.0--25.2 V`；
- 主机油门 `1200--1500 us`；
- 比力模长 `0.3--3.0 g`。

`LOG00106` 只使用 Blackbox 自身时间 `<20.673876 s` 的碰撞前数据。碰撞后的 `19.76 V`、
`1500 us`、`82.06 A` 和冲击过载全部排除，不能用接触后的压降伪造低电压覆盖。

未计入正式覆盖的数据：

|日志|排除原因|
|---|---|
|`LOG00086`|人工自由飞基线，但没有满足当前标定器来源要求的唯一主机 CSV 配对|
|`LOG00090`|现有主机 CSV 未覆盖第二个 Blackbox ARM 段|
|`LOG00094`|Blackbox 内部油门始终为 `1000`，没有 `1200--1500 us` 激励|
|`LOG00097`|没有唯一配对主机 CSV|

## 3. 覆盖结果

### 3.1 发布门槛

|检查项|要求|现有结果|判定|
|---|---:|---:|---|
|有效样本|至少 `500`|`7567`|通过|
|潜在每五个取一留出样本|至少 `100`|`1514`|通过|
|电压范围|`20.0--25.2 V`|`22.25--25.11 V`|不通过|
|油门范围|`1200--1500 us`|`1200--1500 us`|端点通过，但分布不足|
|二维网格|各工作区域有数据|36 格中 21 格为空|不通过|
|中位相对误差|不超过 `10%`|未拟合|未评价|
|P95 相对误差|不超过 `20%`|未拟合|未评价|

所有纳入架次的油门对齐相关系数均为 `0.867--0.999`，超过当前 `0.8` 对齐门槛。样本总量
不是问题，缺少的是工作区间覆盖。

### 3.2 电压分布

|电压区间|10 Hz有效样本|占比|
|---|---:|---:|
|`20--21 V`|0|0%|
|`21--22 V`|0|0%|
|`22--23 V`|176|2.33%|
|`23--24 V`|3793|50.13%|
|`24--25 V`|3554|46.97%|
|`25--25.2 V`|44|0.58%|

现有数据主要覆盖 `23--25 V`。`LOG00106` 接触后的瞬时低压已明确排除，因此当前最低可用
电压是 `22.25 V`，不能宣称覆盖到 `20.0 V`。

### 3.3 油门分布

|油门区间|10 Hz有效样本|占比|
|---|---:|---:|
|`1200--1250 us`|173|2.29%|
|`1250--1300 us`|6365|84.12%|
|`1300--1350 us`|1011|13.36%|
|`1350--1400 us`|8|0.11%|
|`1400--1450 us`|2|0.03%|
|`1450--1500 us`|8|0.11%|

`1500 us` 端点主要由 `LOG00062` 的单次短脉冲提供。它可以证明该点曾经达到，但无法支持跨
电压插值，也无法提供独立架次留出验证。现有数据的 `99.76%` 集中在 `1350 us` 以下。

## 4. 全部历史 Blackbox 复查

为确认更早的飞行是否能补齐缺口，又统一扫描了`logs/blackbox_import/`中的全部`80`个 BFL。
所有文件均成功解码且有`Log clean end`。逐文件机器可读结果见
[`BETAFLIGHT_HISTORICAL_BLACKBOX_AUDIT_20260904.json`](evidence/BETAFLIGHT_HISTORICAL_BLACKBOX_AUDIT_20260904.json)。

### 4.1 总体分类

|分类|数量|解释|
|---|---:|---|
|正式覆盖审计已纳入|15|具有当前标定器要求的主机 CSV/Blackbox 配对；仍按第3节结果计算|
|确认室外场景|18|含15个飞行型、2个极短ARM、1个Blackbox油门无有效激励日志|
|场地不明|53|早期BFL没有有效UTC/GPS或现场记录，不能仅凭动力特征称为室外|
|室内/台架|9|已有实验文档明确场景|
|物理特征像飞行|21|动态飞行、悬停或无约束运动；该数量不能等同于21个室外架次|
|无有效动力激励|50|内部油门没有达到1200 us，或高于idle不足1 s|
|短时/固定试验|9|包括固定台架和`LOG00035`；不能由约束机体IMU反推自由飞推力|

确认室外的飞行型 BFL 为`LOG00086`、`LOG00089--90`、`LOG00095--106`。其中当前正式覆盖
审计没有纳入`LOG00086/90/97`：`LOG00086/97`没有唯一主机 CSV，`LOG00090`只得到部分主机
覆盖。`LOG00087/88`只是极短ARM；`LOG00094`的Blackbox内部油门保持1000，不提供所需激励。

### 4.2 更早的飞行候选

|日志|动力与运动证据|10 Hz内部油门诊断样本|诊断电压|1350 us以上分箱|用途|
|---|---|---:|---:|---:|---|
|`LOG00001`|动力段14.23 s；气压高度变化5.38 m；比力P95 1.91 g；gyro模长P95 71.3 raw|120|22.54--24.36 V|16|补充趋势；无GPS、主机CSV和场景记录|
|`LOG00035`|动力段3.57 s；比力约1 g；gyro和高度几乎不变|23|23.09--23.51 V|9|固定/地面特征，不使用|
|`LOG00055`|动力段29.60 s；气压高度变化7.55 m；比力P95 1.79 g；gyro模长P95 49.0 raw|246|23.51--25.10 V|23|补充趋势；无GPS、主机CSV和场景记录|
|`LOG00057`|已配对的室内人工悬停|295|24.30--24.67 V|0|只增加悬停区重复样本，不补高油门/低电压|

`LOG00001/55`是早期数据中仅有的两个显著自由飞/无约束动态候选。二者合计能提供39个
`1350--1500`内部油门时间桶，因此可用于拟合完成后的独立形状检查；但不能直接进入发布 LUT：

- 旧BFL没有主机`rc_in_ch4`，而Blackbox内部`rcCommand[3]`在端点处不等于接收机物理PWM；
- 两个日志均无GPS，文件内时间为零年，仓库也没有场景口令，不能证明其室外场地和受力条件；
- 筛除`>3 g`冲击后，它们的有效诊断电压下界分别只有`22.54 V`和`23.51 V`，仍没有
  `20--22 V`覆盖。

仓库中的6份`logs/rk3588_orangepi_20260711/`旧主机日志也已核对。5份非空CSV只记录约`4.2 V`
USB信号电、没有ARM动力飞行，另1份为空文件，不能与`LOG00001/55`建立油门配对。

### 4.3 已确认室外但未纳入的飞行

|日志|可用内容|不能补齐的原因|
|---|---|---|
|`LOG00086`|49.14 s人工自由飞、509次GPS更新，可独立验证悬停区动力趋势|内部油门主要1200--1300；无唯一主机CSV|
|`LOG00090`|92.92 s第二次PNG自由飞、908次GPS更新|主机尾段缺失；内部油门最高1341|
|`LOG00097`|14.09 s短飞、142次GPS更新|无同期主机CSV；内部油门最高1313|

这三架可用于交叉检查悬停区域、电压趋势、GPS和飞行动力合理性，但不会增加缺失的
`1350--1500 us`正式物理PWM证据。全部历史Blackbox的内部油门诊断范围为`20.63--25.10 V`，
其中的低压端包含起降/动态瞬态；它不能覆盖正式审计所要求的稳态或可解释受力条件，也不能与第3节
的`7567`个主机PWM样本相加。

历史复查后的结论不变：已有数据量充足，真正缺少的是受控、可配对的低电压/高油门二维覆盖。

## 5. 最小补采方案

### 方案 A：保持 `20.0--25.2 V` 发布范围

只能通过带实际桨叶、可靠固定和测力装置的受控推力试验补齐。至少补：

1. `20--22 V` 下的 `1200--1500 us` 安全工作点；
2. 高、中、低三个电压段的 `1350/1400/1450/1500 us`；
3. 每个区域保留独立重复段，使验证样本不是同一次脉冲的相邻高频行。

普通固定台架只有 IMU、没有测力传感器时不能完成此标定；静止机体的加速度计仍接近 `1 g`，
无法测出约束反力中的真实推力。

### 方案 B：不做低电压带桨试验

根据电池、动力和飞手安全限制，正式收窄主动控制运行电压范围，再补范围内缺失的高油门数据。
当前证据只能支持把候选下界放在约 `22.5 V` 附近、上界放在实际带载可重复达到的约
`25.1 V`；最终数值必须在配置和批准策略中显式修改，不能只改 LUT 文件。

在收窄范围后，由飞手人工控制、程序 `LOG_ONLY`、RC7 人工侧，分别在高、中、低三个允许电压
段做平滑短脉冲，重点补 `1350--1500 us`。不需要重复采集大量 `1250--1300 us` 悬停数据。

无论选择哪个方案，都不得为了取得 `20.0 V` 数据而强行低电压飞行。

## 6. 后续顺序

1. 决定保持 `20.0--25.2 V`，还是正式收窄主动运行电压范围。
2. 只补缺失的电压/高油门组合，并为每个补采架次保留主机 CSV 与唯一 Blackbox。
3. 扩展当前单架次标定器，使其按架次聚合并按整段/整架次划分训练与留出数据，避免相邻样本
   同时进入训练集和验证集。
4. 生成 LUT 后再评价中位误差和 P95 误差；不通过则不能写入监督配置。
5. 通过后记录真实 `calibration_id`、文件 SHA256 和运行电压范围，再重新运行 MC100。

## 7. 复现

```bash
python3 tools/audit_betaflight_thrust_coverage.py \
  --manifest config/betaflight.thrust_coverage_sources.20260904.json \
  --decoder /tmp/betaflight-blackbox-tools/obj/blackbox_decode \
  --output doc/evidence/BETAFLIGHT_THRUST_COVERAGE_AUDIT_20260904.json
```

机器可读结果：
`doc/evidence/BETAFLIGHT_THRUST_COVERAGE_AUDIT_20260904.json`。

全部历史BFL复查：

```bash
DECODED_DIR=$(mktemp -d /tmp/all_blackbox_coverage.XXXXXX)
/tmp/betaflight-blackbox-tools/obj/blackbox_decode \
  --unit-rotation raw --unit-height m --unit-gps-speed mps \
  --merge-gps --save-headers --output-dir "$DECODED_DIR" \
  logs/blackbox_import/LOG*.BFL

python3 tools/audit_betaflight_historical_blackbox.py \
  --decoded-dir "$DECODED_DIR" \
  --blackbox-dir logs/blackbox_import \
  --context config/betaflight.thrust_coverage_sources.20260904.json \
  --output doc/evidence/BETAFLIGHT_HISTORICAL_BLACKBOX_AUDIT_20260904.json
```

本报告是覆盖审计，不是推力 LUT、推力台标定证书或主动控制批准证据。
